# Screen Pop Testing - Environment Configuration

# ============================================================================
# SECURITY - MUST CHANGE IN PRODUCTION
# ============================================================================

# Node environment (development, staging, production)
NODE_ENV=development

# JWT Secret (must be long, random string - CHANGE THIS!)
# Generate with: openssl rand -base64 32
SCREENPOP_JWT_SECRET=dev-secret-key-change-this-in-production

# Allowed origins (comma-separated URLs that can access the API)
# Add your Netlify URL here
SCREENPOP_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://screenpop-testing.netlify.app

# ============================================================================
# FEATURE FLAGS
# ============================================================================

# Enable/disable screen pop testing feature
# Set to 'true' only when testing
SCREENPOP_ENABLED=false

# ============================================================================
# RATE LIMITING
# ============================================================================

# Rate limit window in milliseconds (default: 1 hour = 3600000)
SCREENPOP_RATE_LIMIT_WINDOW=3600000

# Max requests per window per user (default: 100)
SCREENPOP_RATE_LIMIT_MAX=100

# ============================================================================
# LOGGING
# ============================================================================

# Audit log file path
SCREENPOP_AUDIT_LOG=./logs/screenpop-audit.log

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================================================
# DATABASE (if using real CRM later)
# ============================================================================

# Database connection URL
DATABASE_URL=postgresql://user:password@localhost:5432/screenpop_test

# Database user
DB_USER=screenpop_user

# Database password
DB_PASSWORD=secure-password

# ============================================================================
# NOTES
# ============================================================================

# Production checklist:
# 1. Change NODE_ENV to 'production'
# 2. Generate new JWT_SECRET with: openssl rand -base64 32
# 3. Set SCREENPOP_ENABLED to 'false' (enable only for testing)
# 4. Update SCREENPOP_ALLOWED_ORIGINS with only your domain
# 5. Use strong DATABASE_PASSWORD (32+ characters)
# 6. Set up audit log file with proper permissions (700)
# 7. Enable monitoring and alerting
# 8. Test security with: npm run test:security
